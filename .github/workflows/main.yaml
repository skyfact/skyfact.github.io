name: automate generate task

on: 
  push:
    branches:
    - src

jobs:
  build:
    name: build static pages
    runs-on: ubuntu-latest
    steps:
      - name: build
        uses: ./.github/actions/build

